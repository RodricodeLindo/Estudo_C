#include<stdio.h>
#include<math.h>

int main(){
    double a = sqrt(9);//Raiz
    double b = pow(2, 4);//Potência
    int c = round(3.14);//Arredonda pro mais proximo.
    int d = ceil(3.14);//Arrendoda para cima.
    int e = floor(3.9);//Arrendoda para baixo.
    double f = fabs(-100);//Mostra o quão longe o número esta de zero, ou sejá transfomar negativo em positivo.
    double g = log(3);//Logaritmo
    float h = sin(45);//Seno
    float i = cos(45);//Cosseno
    float j = tan(45);//Tangente

    printf("\n%d", a);

    return 0;
}